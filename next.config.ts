import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'd1q14jmvwk39e0.cloudfront.net', 'womo_front.dymain.com'],
  },
};

export default nextConfig;
